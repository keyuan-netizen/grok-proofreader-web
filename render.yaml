services:
  - type: web
    name: grok-proofreader
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROK_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.6